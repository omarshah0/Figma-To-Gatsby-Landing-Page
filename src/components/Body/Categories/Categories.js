import React from "react"
import SectionTitle from "../../Common/SectionTitle/SectionTitle"

function Categories() {
  return (
    <section id="categories">
      <SectionTitle mainTitle="Categories" subTitle="What we have" />
    </section>
  )
}

export default Categories
